import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  viewtext: {
    justifyContent: 'center',
    alignItems: 'center',
    borderWidth: 2,
    width: 65,
    height: 45,
    borderRadius: 4,
  },
  texttop: {
    color: '#98a137',
    fontWeight: 'bold',
    fontSize: 14,
  },
  textbotton: {
    textAlign: 'center',
    width: '98%',
    height: 22,
    maxHeight: '100%',
    backgroundColor: '#fff',
    borderTopWidth: 2,
    paddingTop: 4,
    fontSize: 11,
    fontWeight: 'bold',
  },
});
